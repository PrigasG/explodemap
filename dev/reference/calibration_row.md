# Extract calibration row from an exploded_map

Returns a one-row data.frame suitable for binding across datasets to
build a calibration table.

## Usage

``` r
calibration_row(x)
```

## Arguments

- x:

  An `exploded_map` object

## Value

A one-row data.frame
