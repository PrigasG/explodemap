# Generate initial radial anchors from the national centroid

Generate initial radial anchors from the national centroid

## Usage

``` r
.generate_anchors(
  block_df,
  nat_centroid,
  kappa = 1.8,
  padding = 50000,
  delta = 15000
)
```
