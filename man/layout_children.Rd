% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/child-layout.R
\name{layout_children}
\alias{layout_children}
\title{Lay out child geography around a parent}
\usage{
layout_children(
  x,
  id_col,
  parent = NULL,
  parent_geometry = NULL,
  scale = 1.6,
  radial_kick = NULL,
  gap = NULL,
  collision = c("bbox", "circle"),
  attraction = 0.08,
  max_bearing_drift = 0.22,
  bounds = NULL,
  max_iter = 180L
)
}
\arguments{
\item{x}{Projected polygon \code{sf} object containing child features.}

\item{id_col}{Stable unique feature identifier column.}

\item{parent}{Optional single parent identifier recorded in the output.}

\item{parent_geometry}{Optional parent \code{sf}, \code{sfc}, or \code{sfg} geometry whose
representative point anchors the expansion. Defaults to the union of \code{x}.}

\item{scale}{Radial expansion multiplier applied to source vectors.}

\item{radial_kick}{Additional outward movement in projected map units.
Defaults to two percent of the child layer's bounding-box diagonal.}

\item{gap}{Minimum separation in projected map units. Defaults to one
percent of the child layer's bounding-box diagonal.}

\item{collision}{Collision approximation: feature bounding boxes or
centroid circles derived from those boxes.}

\item{attraction}{Fraction of the remaining distance to the geographic
target applied during each refinement iteration.}

\item{max_bearing_drift}{Maximum angular drift from the source bearing, in
radians.}

\item{bounds}{Optional bounding box supplied as an \code{sf}/\code{sfc} object,
\code{st_bbox}, or numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{max_iter}{Maximum collision-refinement iterations.}
}
\value{
An \code{explodemap_child_layout} with \code{offsets}, composed \code{geometry},
source geometry, diagnostics, and parameters. \code{offsets} contains stable
IDs, source/final anchors, base offsets, distance, bearing, bearing drift,
and residual collision counts.
}
\description{
Computes a renderer-neutral child expansion in projected map units. Child
features move radially away from the parent anchor, then a bounded
axis-aligned bounding-box or circle solver separates collisions while an
attraction term and bearing guard preserve the source geography's mental
map. The returned base offsets are algorithmic movement; manual/editorial
movement belongs in a \code{dragmapr_state} and can be composed downstream.
}
