% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_quality.R
\name{as_dragmapr_state}
\alias{as_dragmapr_state}
\title{Convert a grouped layout to a dragmapr editing state}
\usage{
as_dragmapr_state(result, geometry_id = NULL, level = NULL)
}
\arguments{
\item{result}{A \code{grouped_exploded_map}.}

\item{geometry_id}{Optional single string identifying the source geometry.
Defaults to the layout label in \code{result$diagnostics$label}.}

\item{level}{Optional geography-level label. Defaults to the region column.}
}
\value{
A \code{dragmapr_state} (see \code{dragmapr::d_state()}).
}
\description{
Produces a \code{dragmapr::d_state()} -- the shared editorial composition
contract -- from a \code{grouped_exploded_map}. Unlike \code{\link[=as_dragmapr]{as_dragmapr()}}, which
bundles geometry and absolute anchors into a handoff list, this emits the
geometry-free \emph{state} that the interactive editor and static renderers
consume: the exploded anchors (\code{anchor_x}/\code{anchor_y}) are converted to the
metre deltas (\code{dx_m}/\code{dy_m}) relative to the current region centroids that
\code{dragmapr} expects, and the projected CRS plus a provenance \code{geometry_id}
are recorded so the state can be reapplied safely in a later session.
}
\details{
Keep \code{layout} (the computed geometry + diagnostics, i.e. \code{result}) and
\code{state} (this editorial overlay) separate. The layout can be recomputed
freely while the state preserves manual edits, which is what makes the
\code{compute -> compose -> render} workflow reproducible.
}
