% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as-sf.R
\name{as_sf}
\alias{as_sf}
\title{Extract plain sf geometry from an exploded layout}
\usage{
as_sf(layout, which = c("exploded", "original"))
}
\arguments{
\item{layout}{An \code{exploded_map} or \code{grouped_exploded_map} object.}

\item{which}{Which stored geometry layer to return. For \code{exploded_map}:
\code{"exploded"} (default) or \code{"original"}. For \code{grouped_exploded_map}:
\code{"grouped"} (default), \code{"local"}, or \code{"original"}.}
}
\value{
A plain \code{sf} object with the selected geometry layer active. All
attribute columns and the CRS are kept as stored. Layout metadata
(\code{stats}, \code{params}, \code{plots}, \code{refinement}, \code{diagnostics}, \code{anchors}) is
dropped. The \code{"exploded"}/\code{"grouped"} layers carry the displaced
geometries; \code{"original"} carries the input geometries.
}
\description{
Converts an \code{exploded_map} or \code{grouped_exploded_map} back to a plain \code{sf}
data frame so the layout can flow into downstream GIS work (writing files,
spatial joins, further \code{sf} analysis) without carrying the layout metadata.
}
\examples{
\donttest{
sq <- function(xmin, ymin, size = 1) {
  sf::st_polygon(list(matrix(
    c(xmin, ymin, xmin + size, ymin, xmin + size, ymin + size,
      xmin, ymin + size, xmin, ymin),
    ncol = 2, byrow = TRUE
  )))
}
x <- sf::st_sf(
  id = c("a1", "a2", "b1", "b2"),
  region = c("A", "A", "B", "B"),
  geometry = sf::st_sfc(sq(0, 0), sq(2, 0), sq(10, 0), sq(12, 0), crs = 3857)
)
layout <- explode_sf(x, region_col = "region", plot = FALSE, quiet = TRUE)

# Displaced geometries, ready for st_write() or further analysis
exp_sf <- as_sf(layout)
sf::st_crs(exp_sf)

# The untouched input geometries
orig_sf <- as_sf(layout, which = "original")
}
}
