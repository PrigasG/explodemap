% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_quality.R
\name{optimize_grouped_layout}
\alias{optimize_grouped_layout}
\title{Search grouped-layout parameters}
\usage{
optimize_grouped_layout(
  x,
  region_col,
  objective = c("no_overlap", "compact", "balanced", "stable"),
  weights = layout_objective(),
  label_col = NULL,
  label_size = NULL,
  grid = NULL,
  kappa = c(1.4, 1.8, 2.2),
  padding = c(25000, 50000, 75000),
  delta = c(5000, 15000, 30000),
  padding_sep = c(10000, 20000, 40000),
  ...
)
}
\arguments{
\item{x}{Projected sf object with region column.}

\item{region_col}{Grouping column name.}

\item{objective}{Objective presets: \code{"no_overlap"}, \code{"compact"},
\code{"balanced"}, and/or \code{"stable"}.}

\item{weights}{Named weights from \code{\link[=layout_objective]{layout_objective()}}.}

\item{label_col, label_size}{Optional label column and approximate label size.
When \code{label_col} is supplied the search becomes label-aware: each candidate
layout is scored for approximate label overlaps (weighted by the
\code{label_overlap} term of \code{weights}). Left \code{NULL} (the default), label
overlaps contribute zero, so the search result is identical to before.}

\item{grid}{Optional data.frame of parameter combinations.}

\item{kappa, padding, delta, padding_sep}{Candidate values used when \code{grid} is
\code{NULL}.}

\item{...}{Passed to \code{\link[=explode_grouped]{explode_grouped()}}.}
}
\value{
A \code{grouped_exploded_map} with \code{optimization} metadata.
}
\description{
Search grouped-layout parameters
}
