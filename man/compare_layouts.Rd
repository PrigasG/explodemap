% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as-sf.R
\name{compare_layouts}
\alias{compare_layouts}
\title{Compare displacement between two exploded layouts}
\usage{
compare_layouts(before, after)
}
\arguments{
\item{before, after}{\code{exploded_map} objects (or both
\code{grouped_exploded_map} objects) to compare.}
}
\value{
A list with two elements:
\itemize{
\item \code{per_feature}: \code{data.frame} with one row per feature and columns
\code{distance_before}, \code{distance_after}, and \code{delta} (after minus before),
in the layout's CRS units.
\item \code{summary}: \code{data.frame} with one row per layout (\code{"before"}, \code{"after"})
and columns \code{total}, \code{mean}, and \code{max} displacement.
}
}
\description{
Summarises how per-feature displacement changes between two layouts of the
same input (e.g. different \code{alpha_r}/\code{alpha_l}/\code{gamma} choices or
before/after collision refinement). Both layouts must be of the same class
and cover the same features in the same row order.
}
\examples{
\donttest{
sq <- function(xmin, ymin, size = 1) {
  sf::st_polygon(list(matrix(
    c(xmin, ymin, xmin + size, ymin, xmin + size, ymin + size,
      xmin, ymin + size, xmin, ymin),
    ncol = 2, byrow = TRUE
  )))
}
x <- sf::st_sf(
  id = c("a1", "a2", "b1", "b2"),
  region = c("A", "A", "B", "B"),
  geometry = sf::st_sfc(sq(0, 0), sq(2, 0), sq(10, 0), sq(12, 0), crs = 3857)
)
small <- explode_sf(x, region_col = "region", alpha_r = 10, alpha_l = 10,
                    plot = FALSE, quiet = TRUE)
big <- explode_sf(x, region_col = "region", alpha_r = 100, alpha_l = 100,
                  plot = FALSE, quiet = TRUE)
cmp <- compare_layouts(small, big)
cmp$summary
}
}
