% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focusmap.R
\name{focusMapProxy}
\alias{focusMapProxy}
\alias{update_focus_labels}
\alias{update_focus_palette}
\alias{update_focus_data}
\title{Focus-map Shiny proxy}
\usage{
focusMapProxy(outputId, session = NULL)

update_focus_labels(proxy, show = TRUE)

update_focus_palette(proxy, palette)

update_focus_data(proxy, data, ...)
}
\arguments{
\item{outputId}{Shiny output ID for an existing \code{focus_map()}.}

\item{session}{Shiny session. Defaults to the current reactive domain.}

\item{proxy}{A \code{focusmap_proxy}.}

\item{show}{Logical; show focus labels.}

\item{palette}{Named character vector of group colours.}

\item{data}{New \code{sf}, \code{exploded_map}, or \code{grouped_exploded_map} data.}

\item{...}{Passed to \code{\link[=focus_map]{focus_map()}} when building the replacement payload.}
}
\value{
A \code{focusmap_proxy} object.
}
\description{
Focus-map Shiny proxy
}
\details{
\code{update_focus_data()} swaps the widget's geometry and rebuilds it, which
returns the map to its idle (unfocused) view -- a data swap is treated as a
fresh dataset. To keep the user focused on a feature across the swap, pass a
\code{state} and \code{restore_selection = TRUE} through \code{...}; these flow to
\code{\link[=focus_map]{focus_map()}} and the new feature is re-focused on load:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{update_focus_data(proxy, new_sf, id_col = "GEOID",
                  state = state, restore_selection = TRUE)
}\if{html}{\out{</div>}}
}
