% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout_quality.R
\name{as_dragmapr}
\alias{as_dragmapr}
\title{Convert a grouped layout to a drag-edit handoff object (legacy)}
\usage{
as_dragmapr(result)
}
\arguments{
\item{result}{A \code{grouped_exploded_map}.}
}
\value{
A \code{dragmapr_layout} list containing editable sf geometry and anchors.
}
\description{
Legacy / low-level handoff (superseded, but not deprecated).

This is the original handoff: it bundles the grouped geometry and
the absolute anchors into a \code{dragmapr_layout} list. The preferred path is now
\code{\link[=as_dragmapr_state]{as_dragmapr_state()}}, which emits a geometry-free \code{dragmapr::d_state()}
-- the shared editorial composition contract that \verb{state =} arguments accept
across \code{focus_map()}, \code{render_dragged_map()}, and \code{update_exploded_layout()}.

\code{as_dragmapr()} remains supported for existing offset-table / CSV workflows
and is not scheduled for removal, but new code should prefer the state-first
API.
}
\seealso{
\code{\link[=as_dragmapr_state]{as_dragmapr_state()}} for the preferred state-first handoff.
}
