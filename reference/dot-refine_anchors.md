# Refine anchors with pairwise repulsion plus spring return

Refine anchors with pairwise repulsion plus spring return

## Usage

``` r
.refine_anchors(
  block_df,
  lambda = 0.18,
  eta = 0.18,
  padding_sep = 20000,
  max_iter = 60
)
```
