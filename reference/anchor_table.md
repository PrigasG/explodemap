# Access grouped-layout anchor data

Access grouped-layout anchor data

## Usage

``` r
anchor_table(layout)
```

## Arguments

- layout:

  A `grouped_exploded_map`.

## Value

A data frame with one row per group anchor.
