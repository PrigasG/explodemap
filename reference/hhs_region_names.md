# HHS region display names

HHS region display names

## Usage

``` r
hhs_region_names(short = FALSE)
```

## Arguments

- short:

  Logical. When `TRUE`, returns labels such as `"HHS 1"`; otherwise
  returns labels such as `"1 - Boston"`.

## Value

A named character vector keyed by region id.
